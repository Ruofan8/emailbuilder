import { DragTargetDirective } from './drag-target.directive';

describe('DragTargetDirective', () => {
  it('should create an instance', () => {
    const directive = new DragTargetDirective();
    expect(directive).toBeTruthy();
  });
});
